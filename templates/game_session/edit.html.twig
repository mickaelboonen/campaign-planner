{% extends 'base.html.twig' %}

{% trans_default_domain 'game_session' %}

{% block title %}
    {{ 'edit.title'|trans }} — {{ campaign.name }}
{% endblock %}

{% block body %}
    {% include 'components/breadcrumb.html.twig' with {
        items: [
            {
                label: 'breadcrumb.campaigns'|trans({}, 'campaign'),
                path: path('campaign_list')
            },
            {
                label: campaign.name,
                path: path('campaign_show', {
                    id: campaign.id
                })
            },
            {
                label: 'edit.title'|trans
            }
        ]
    } %}

    {% include 'components/page-header.html.twig' with {
        eyebrow: campaign.name,
        title: 'edit.title'|trans,
        description: 'edit.description'|trans
    } %}

    {% include 'game_session/_form.html.twig' with {
        campaign: campaign,
        form: form,
        sessionDate: session.date,
        sessionPeriod: session.period,
        submitLabel: 'edit.submit'|trans
    } %}
{% endblock %}
