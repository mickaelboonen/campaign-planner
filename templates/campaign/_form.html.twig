{% trans_default_domain 'campaign' %}

<section class="campaign-form-card">
    <header class="campaign-form-card__header">
        <div>
            <p class="page-eyebrow">
                {{ 'form.eyebrow'|trans }}
            </p>

            <h2>
                {{ form_title }}
            </h2>

            <p>
                {{ 'form.description'|trans }}
            </p>
        </div>
    </header>

    {{ form_start(form) }}

        <div class="campaign-form">
            <div class="campaign-form__field">
                {{ form_label(
                    form.name,
                    'form.name.label'|trans
                ) }}

                {{ form_widget(form.name, {
                    attr: {
                        placeholder: 'form.name.placeholder'|trans
                    }
                }) }}

                {{ form_errors(form.name) }}
            </div>

            <div class="campaign-form__field">
                {{ form_label(
                    form.color,
                    'form.color.label'|trans
                ) }}

                <div class="campaign-form__color">
                    {{ form_widget(form.color) }}

                    <span class="campaign-form__color-hint">
                        {{ 'form.color.hint'|trans }}
                    </span>
                </div>

                {{ form_errors(form.color) }}
            </div>

            <div class="campaign-form__field">
                {{ form_label(
                    form.imageFile,
                    'form.image.label'|trans
                ) }}

                {% if campaign is defined and campaign.image %}
                    <div class="campaign-form__current-image">
                        <img
                            src="{{ asset(
                                'uploads/campaigns/' ~ campaign.image
                            ) }}"
                            alt=""
                        >

                        <span>
                            {{ 'form.image.current'|trans }}
                        </span>
                    </div>
                {% endif %}

                {{ form_widget(form.imageFile) }}
                {{ form_errors(form.imageFile) }}

                <p class="campaign-form__help">
                    {{ 'form.image.help'|trans }}
                </p>
            </div>
        </div>

        <div class="campaign-form__actions">
            <a
                class="button button--secondary"
                href="{{ cancel_path }}"
            >
                {{ 'form.cancel'|trans }}
            </a>

            <button
                type="submit"
                class="button button--primary"
            >
                {{ submit_label }}
            </button>
        </div>

    {{ form_end(form) }}
</section>
