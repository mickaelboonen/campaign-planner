{% trans_default_domain 'participant_access' %}

{% if not isPastWeek %}
    <div class="availability-save">
        <p
            class="availability-save__status"
            data-availability-grid-target="saveStatus"
            aria-live="polite"
        ></p>
    </div>
{% endif %}
