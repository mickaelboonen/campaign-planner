{% extends 'portal_base.html.twig' %}

{% trans_default_domain 'auth' %}

{% block title %}
    {{ 'login.title'|trans }}
{% endblock %}

{% block portal_class %}
    portal-page--auth
{% endblock %}

{% block portal_content %}
    <nav class="auth-tabs">
        <a
            href="{{ path('app_login') }}"
            class="auth-tabs__item is-active"
        >
            {{ 'tabs.login'|trans }}
        </a>

        <a
            href="{{ path('app_register') }}"
            class="auth-tabs__item"
        >
            {{ 'tabs.register'|trans }}
        </a>
    </nav>

    {% if error %}
        <div
            class="portal-message portal-message--error"
            role="alert"
        >
            {{ error.messageKey|trans(
                error.messageData,
                'security'
            ) }}
        </div>
    {% endif %}

    <form
        method="post"
        class="form"
    >
        <div class="form-row">
            <label
                class="form-label"
                for="username"
            >
                {{ 'login.email'|trans }}
            </label>

            <input
                class="form-control"
                type="email"
                value="{{ last_username }}"
                name="_username"
                id="username"
                autocomplete="email"
                required
                autofocus
            >
        </div>

        <div class="form-row">
            <label
                class="form-label"
                for="password"
            >
                {{ 'login.password'|trans }}
            </label>

            <input
                class="form-control"
                type="password"
                name="_password"
                id="password"
                autocomplete="current-password"
                required
            >
        </div>

        <input
            type="hidden"
            name="_csrf_token"
            data-controller="csrf-protection"
            value="{{ csrf_token('authenticate') }}"
        >

        <button
            class="button button--portal"
            type="submit"
        >
            {{ 'login.submit'|trans }}
        </button>
    </form>

    <div class="player-access-link">
        <span>
            {{ 'login.player_question'|trans }}
        </span>

        <a href="{{ path('participant_recover') }}">
            {{ 'login.player_recover'|trans }}
        </a>
    </div>

    <div class="portal-secondary-link">
        <a href="{{ path('feedback_create') }}">
            {{ 'login.contact_admin'|trans }}
        </a>
    </div>
{% endblock %}
