{% trans_default_domain 'emails' %}

<table
    role="presentation"
    cellspacing="0"
    cellpadding="0"
    border="0"
    align="center"
>
    <tr>
        <td class="email-button-wrapper">
            <a
                href="{{ dashboardUrl }}"
                class="email-button"
            >
                {{ 'dashboard_button.label'|trans }}
            </a>
        </td>
    </tr>
</table>
